// @ts-check

/**
 * Directory Manager 11.1 – Sidebar
 *
 * The structure intentionally mirrors the new, flatter folder hierarchy.
 * We hand-place the root *Overview* page and rely on the *autogenerated*
 * feature for everything else so that future additions automatically appear
 * in navigation as long as the content writer places the file in the correct
 * directory.
 */

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  directoryManager111Sidebar: [
    {
      type: 'doc',
      id: 'index',
      label: 'Directory Manager 11.1 Overview',
    },
    {
      type: 'autogenerated',
      // root of docs/directorymanager/11.1
      dirName: '.',
    },
  ],
};

module.exports = sidebars;
